{
    "name": "movie-mogul",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "test": "vitest",
        "coverage": "vitest run --coverage",
        "test:e2e": "playwright test",
        "test:e2e:ui": "playwright test --ui",
        "test:e2e:headed": "playwright test --headed",
        "deploy": "npm run build && rsync -av --delete dist/ ../mcornell.dev/public/games/movie-mogul/"
    },
    "devDependencies": {
        "@playwright/test": "^1.58.2",
        "@vitest/coverage-v8": "^4.0.18",
        "typescript": "^5.9.0",
        "vite": "^7.3.1",
        "vitest": "^4.0.18"
    }
}
